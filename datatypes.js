// Primitive data-types(call by value)
// string,number,boolean,null,undefined,symbol(to create variable unique),BigInt.

//Refference type Or non-primitive(by allocate refference)
//Array,object,Functions

const name = ["jahir", "hasan"];
let myObj = {
    name: "jahir",
    age: 23,
}

const myFunction = function(){
    console.log("non-primitive data-type");
}